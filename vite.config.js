import { defineConfig } from 'vite'

export default defineConfig({
  base: '/portfolio/',
  server: {
    open: true,
  },
})

// Update if requiring
// -path aliases
// -proxy rules
// -custom base path
// -plugins
// -build tweaks